<h2>Writings</h2>
<ul>
  *{info.writings.reverse as w}{
    <li class="?{w.identifier == current}{current}{normal}"><a href="@{${w.identifier}}">
      ?{w.identifier == current}{&raquo;&nbsp;}${w.short || w.title}
    </a></li> 
  }
</ul>
